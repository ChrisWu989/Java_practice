public class TestClass extends Thread
{
    class Runner implements Runnable
    {
        public void run()
        {
            var t = new Thread[5]; // Array of threads Refference
            for(int i=0; i<t.length; i++) System.out.println(t[i]); // Prints content of threads
        }
    }
    
   public static void main(String args[]) throws Exception
   {
            var tc = new TestClass();
            new Thread( tc.new Runner() ).start(); 
            // calling a new Runner
            // Does not call TestClass

            // Main of 1 Thread than 2 threads from tx.new
   }
}